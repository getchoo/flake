builds:
  exclude:
    - "*.x86_64-darwin.*"
    - "*.aarch64-darwin.*"
  include:
    - "checks.*.*"
    - "devShells.*.*"
    - "nixosConfigurations.*"
    - "homeConfigurations.*"
    - "packages.*.*"
